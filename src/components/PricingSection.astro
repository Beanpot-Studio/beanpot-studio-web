---
const pricingPlans = [
  {
    name: "Starter",
    price: "49",
    description: "Perfect for individual educators or small classrooms.",
    features: [
      "Up to 30 student accounts",
      "Basic analytics",
      "5 project templates",
      "Community support",
      "Standard content library",
    ],
    cta: "Get Started",
    popular: false
  },
  {
    name: "Professional",
    price: "99",
    description: "Ideal for schools and educational departments.",
    features: [
      "Up to 500 student accounts",
      "Advanced analytics & reporting",
      "15 project templates",
      "Priority email support",
      "Expanded content library",
      "Custom branding",
      "Assessment tools"
    ],
    cta: "Get Started",
    popular: true
  },
  {
    name: "Enterprise",
    price: "Custom",
    periodText: "contact us for pricing", 
    description: "For large institutions and educational platforms.",
    features: [
      "Unlimited student accounts",
      "Enterprise-grade analytics",
      "Unlimited project templates",
      "24/7 dedicated support",
      "Full content library",
      "Custom development",
      "Advanced assessment tools",
      "API access",
      "Single sign-on"
    ],
    cta: "Contact Sales",
    popular: false
  }
];
---

<section id="pricing" class="section-padding bg-primary-50">
  <div class="container-custom">
    <div class="text-center max-w-3xl mx-auto mb-16">
      <span class="inline-block px-3 py-1 text-sm font-medium bg-secondary-100 text-secondary-800 rounded-full mb-4">
        Pricing Plans
      </span>
      <h2 class="text-3xl md:text-4xl font-bold mb-6 text-primary-950">
        Find the perfect plan for your educational needs
      </h2>
      <p class="text-lg text-primary-800">
        Whether you're an individual educator or a large institution, we have flexible pricing options to meet your requirements.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      {pricingPlans.map((plan, index) => (
        <div class={`card relative ${plan.popular ? 'ring-2 ring-sage-500' : ''}`}>
          {plan.popular && (
            <div class="absolute -top-4 left-1/2 transform -translate-x-1/2 bg-sage-600 text-btn-on px-4 py-1 rounded-full text-sm font-medium">
              Most Popular
            </div>
          )}
          
          <div class="mb-6">
            <h3 class="text-xl font-bold mb-2 text-primary-950">{plan.name}</h3>
            <p class="text-primary-800 mb-4">{plan.description}</p>
            
            <div class="flex items-baseline mb-6">
              <span class="text-4xl font-bold">${plan.price}</span>
              {plan.periodText ? (
                <span class="ml-2 text-primary-800">{plan.periodText}</span>
              ) : (
                <span class="ml-2 text-primary-800">/month</span>
              )}
            </div>
          </div>
          
          <ul class="space-y-3 mb-8">
            {plan.features.map((feature) => (
              <li class="flex items-start">
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5 text-success-500 mr-2 flex-shrink-0 mt-0.5">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
                <span>{feature}</span>
              </li>
            ))}
          </ul>
          
          <a 
            href="/contact" 
            class={`w-full min-h-11 text-center items-center inline-flex justify-center py-2.5 px-4 rounded-xl font-semibold text-[15px] transition-colors ${
              plan.popular 
                ? 'bg-sage-600 text-btn-on shadow-craft hover:bg-sage-700 hover:shadow-craft-sm' 
                : 'bg-white border-2 border-primary-200 text-primary-800 hover:bg-primary-50'
            }`}
          >
            {plan.cta}
          </a>
        </div>
      ))}
    </div>
    
    <div class="mt-16 bg-white rounded-2xl shadow-soft p-8 md:p-10">
      <div class="md:flex items-center justify-between">
        <div class="mb-6 md:mb-0 md:mr-8">
          <h3 class="text-2xl font-bold mb-2">Need a custom solution?</h3>
          <p class="text-primary-800 max-w-2xl">
            We offer tailored solutions for educational institutions with specific requirements. Contact our sales team to discuss your needs.
          </p>
        </div>
        <a href="/contact" class="btn-primary whitespace-nowrap">Contact Sales</a>
      </div>
    </div>
  </div>
</section>